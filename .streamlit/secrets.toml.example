# Streamlit Secrets Example
# Copy this to secrets.toml for local development
# For Streamlit Cloud: Settings > Secrets

# This app doesn't require any secrets/API keys
# Add custom configuration here if needed in the future

[app]
name = "MF FAQ Assistant"
version = "1.0.0"
